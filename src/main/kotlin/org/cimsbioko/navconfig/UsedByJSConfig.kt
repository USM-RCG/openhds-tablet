package org.cimsbioko.navconfig

/**
 * Used to mark code used so we don't accidentally delete it using IDE inspections. It is also referenced in the build
 * so that proguard doesn't remove code intended to be referenced by scripting.
 */
annotation class UsedByJSConfig 