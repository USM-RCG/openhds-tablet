"use strict";

/*
  A default configuration to be used as a fallback. If a campaign is defined on external storage or downloaded from
  the server at initial setup, this will not be used.
 */

exports.hierarchy = require('hierarchy').hierarchy;
exports.navmods = [];
